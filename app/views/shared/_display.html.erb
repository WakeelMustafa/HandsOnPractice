 <tr>

      <td><%= student.id%></td>
      <td><%= student.name%></td>
      <td><%= student.age%></td>
      <td>
       <div class = "btn-group">
      <%=button_to 'Details',student_path(student), method: 'get', class: "btn btn-info"%>
      <%= button_to 'Delete', student_path(student), method: 'delete', class: "btn btn-danger" %>
      <%= button_to 'Edit', edit_student_path(student), method: 'get', class: "btn btn-secondary"%>
      </div>
</td>

    </tr>
